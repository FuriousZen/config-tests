Do NOT modify any code. Answer a question about this codebase.

Trace the complete control flow that happens when a user types a title into the
`TaskForm` and submits it to create a new task. Name every function, hook,
component, and reducer action that runs, in order, across all files — from the
form submit handler, through validation and the context/service/api layers, into
the store, and back out to where the new task becomes visible on the board.

For each step name the specific file and symbol (e.g.
`app/components/TaskForm/TaskForm.tsx:handleSubmit`) and say briefly what it does
and what it returns or dispatches.
